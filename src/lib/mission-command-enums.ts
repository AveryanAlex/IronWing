export const LOITER_DIRECTIONS = ["Clockwise", "CounterClockwise"] as const;
export const ALT_CHANGE_ACTIONS = ["Neutral", "Climb", "Descend"] as const;
export const SPEED_TYPES = ["Airspeed", "Groundspeed"] as const;
export const FENCE_ACTIONS = ["Disable", "Enable", "DisableFloor"] as const;
export const PARACHUTE_ACTIONS = ["Disable", "Enable", "Release"] as const;
export const GRIPPER_ACTIONS = ["Release", "Grab"] as const;
export const WINCH_ACTIONS = ["Relax", "LengthControl", "RateControl"] as const;
export const YAW_DIRECTIONS = ["Clockwise", "CounterClockwise"] as const;
