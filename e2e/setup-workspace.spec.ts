import { test } from "./fixtures/mock-platform";

test.describe.skip("setup workspace proof", () => {
    test("placeholder until T04 lands the end-to-end setup lifecycle proof", async () => {
        // Intentionally empty. T04 replaces this skipped placeholder with the real setup proof flow.
    });
});
